{% extends 'partials/page.twig' %}
{% block title %}{{ trans('signup.title') }}{% endblock %}
{% block head %}
    {{ parent() }}
    <meta http-equiv="refresh" content="3; url={{ links.checkout.href }}"/>
{% endblock %}
{% block content %}
    <div class="container content">
        <main class="signup">
            <h1>{{ trans('signup.title') }}</h1>
            <div class="row justify-content-center">
                <div class="col-md-4 payment-page">
                    <div class="loader">
                        <span></span>
                        <span></span>
                        <span></span>
                    </div>
                    <h3>{{ trans('signup.redirecting') }}</h3>
                    <p>{{ trans('signup.redirecting_instructions') }}</p>
                </div>
            </div>
        </main>
    </div>
{% endblock %}