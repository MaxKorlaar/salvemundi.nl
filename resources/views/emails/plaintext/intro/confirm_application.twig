{{ trans('email.intro.confirm_application.greeting', { name: application.first_name }) }}


{{ trans('email.intro.confirm_application.instructions') }}


{{ route('intro.signup.confirm_email', { id: application.id, token: application.email_confirmation_token }) }}

{{ trans('intro.signup.instructions', { amount: application.calculateIntroCosts(), id: application.transaction_id })|striptags }}

{{ trans('email.signature') }}

---
{{ trans('email.disclaimer') }}