{% set nav_background = false %}
{% extends 'partials/page.twig' %}
{% block title %}{{ trans('home.title') }}{% endblock %}
{% block container %}
    {% block landing %}
        <section class="landing">
            <h1>Welkom bij Salve Mundi</h1><br>
            <h5>Meld je vandaag nog aan!</h5><br>
            <a class="signup-btn button" href="{{ route('signup') }}">{{ trans('home.signup_link') }}</a>
        </section>
    {% endblock %}
    {% block content %}
        <div class="container-fluid content index">
            <main>
                <section class="desc container">
                    <p>Welkom op de (tijdelijke) website van Salve Mundi! Tot op heden is het enkel mogelijk in te
                       schrijven, maar verwacht in de nabije toekomst een uitgebreide website vol informatie.<br/>Salve
                       Mundi is de nieuwe studievereniging van Fontys Hogescholen ICT, opgericht in 2017 door Mohammed
                       Mighiss en Luuk Hendriks. Het is Latijn voor &ldquo;Hello World&rdquo;, een typische zin die menig
                       programmeur maar al te goed kent.</p>
                    <p>De vereniging organiseert veel activiteiten, zowel studie bemiddelend als voor de lol, of een
                       combinatie van de twee. Denk hierbij aan:&nbsp;</p>
                    <ul>
                        <li>Lezingen</li>
                        <li>Excursies</li>
                        <li>Workshops</li>
                        <li>Bedrijven bezoeken</li>
                        <li>Elke woensdag bijlessen</li>
                    </ul>
                    <p>Voor de uitgaande studenten:</p>
                    <ul>
                        <li>Op stap bij onze stamkroeg Villa Fiesta&nbsp;</li>
                        <li>Casinoavonden</li>
                        <li>Pubquiz-avonden</li>
                        <li>Willekeurige spontane activiteiten</li>
                    </ul>
                    <p>En voor de wat minder uitgaande studenten:</p>
                    <ul>
                        <li>Jaarlijks op kamp</li>
                        <li>Game Nights (bordspellen&nbsp;&amp;&nbsp;eSports)</li>
                        <li>Partnership met een grote hackathon</li>
                        <li>Uitjes naar bijvoorbeeld pretparken</li>
                    </ul>
                    <p>Naast deze activiteiten verzorgt Salve Mundi ook de introductie van FHICT van top tot teen.</p>
                    <a href="{{ route('signup') }}" class="button">{{ trans('home.signup_link') }}</a>
                </section>
                <section class="events" id="events" data-url="{{ route('facebook_events') }}">
                    <div class="container">
                        <h1>{{ trans('home.events.upcoming') }}</h1>
                        <div class="loading text-center" v-if="loading">
                            <h3>{{ trans('home.events.loading') }}</h3>
                        </div>
                        <div v-cloak class="error text-center" v-if="error">
                            <h3>{{ trans('home.events.error') }}</h3>
                            <p>{{ trans('home.events.try_again_later') }}</p>
                        </div>
                        <a :href="event.url" target="_blank" v-cloak class="event" v-for="event in events" :class="{ background: event.cover }" :data-background="event.cover ? event.cover.source : null">
                            <div class="background" v-if="event.cover" :style="{ backgroundImage: 'url(' + event.cover.source + ')', backgroundPositionY: -event.cover.offset_y + 'px' }"></div>
                            <div class="date-container">
                                <div class="date">
                                    <span class="day">{{ "{{ event.date.day }}" }}</span>
                                    <span class="month">{{ "{{ event.date.month }}" }}</span>
                                </div>
                                <div class="place" v-if="event.place">
                                    {{ "{{ event.place.name }}" }}
                                </div>
                            </div>
                            <div class="event-content">
                                <h3>{{ "{{ event.name }}" }}</h3>
                                <p>
                                    <template v-for="line in event.description.split('\n')">
                                        {{ "{{ line }}" }}<br>
                                    </template>
                                </p>
                            </div>
                        </a>
                    </div>
                </section>

                <section class="committees container" id="committees">
                    <h3>{{ trans('home.committees.heading') }}</h3>
                    <p>{{ trans('home.committees.description') }}</p>
                    <hr>
                    <div class="buttons">
                        <a href="{{ route('committees/administration') }}" class="button">{{ trans('committee.administration.title') }}</a>
                        <a href="#" class="button">{{ trans('committee.party.title') }}</a>
                        <a href="#" class="button">{{ trans('committee.media.title') }}</a>
                        <a href="#" class="button">{{ trans('committee.camping.title') }}</a>
                        <a href="#" class="button">{{ trans('committee.women.title') }}</a>
                    </div>
                </section>
            </main>
        </div>
    {% endblock %}
{% endblock %}
{% block scripts %}
    <script src="{{ mix('js/manifest.js') }}"></script>
    <script src="{{ mix('js/vendor.js') }}"></script>
    <script src="{{ mix('js/landing.js') }}"></script>
{% endblock %}