{% extends 'partials/page.twig' %}
{% block title %}{{ trans('committee.camping.title') }}{% endblock %}
{% block content %}
    <div class="content">
        <main class="committee camping-committee">
            <div class="container">
                <h1>{{ trans('committee.camping.title') }}</h1>
                <section>
                    <p>{{ trans('committee.camping.about_text') }}</p>
                </section>
            </div>
            <section class="members">
                <div class="container">
                    <h2>{{ trans('committee.camping.meet_the_members') }}</h2>
                    <div class="grid">
                        {% for member in trans('committee.camping.members') %}
                            <div class="member has-image" id="{{ member.name|str_slug }}">
                                <div class="inner {{ member.reverse ? 'reverse' }}">
                                    <div class="content">
                                        <h4>{{ member.name }}</h4>
                                        <h5>{{ member.profile }}</h5>
                                    </div>
                                    <div class="image">
                                        <div class="img" style="background-image: url('{{ asset('images/commissies/kamp/' ~ member.image) }}');">
                                        </div>
                                    </div>
                                </div>
                            </div>
                        {% endfor %}
                    </div>
                </div>
            </section>

        </main>
    </div>
{% endblock %}