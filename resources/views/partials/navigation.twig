<header class="{{ nav_background is defined ? nav_background == true ? '' : 'no-background' }}">
    <nav>
        <div class="container">
            <a href="{{ route('home') }}" class="logo">
                <img src="{{ asset('images/logo.svg') }}" alt="Salve Mundi">
            </a>
            <ul>
                {#<li>#}
                {#<a href="{{ route('intro.info') }}">{{ trans('nav.intro') }}</a>#}
                {#</li>#}
                {#<li>#}
                {#<a href="{{ route('signup.signup') }}">{{ trans('nav.sign_up') }}</a>#}
                {#</li>#}
                {% if auth_user() %}
                    <li>
                        <a href="{{ route('member.about_me') }}">{{ trans('nav.my_account') }}</a>
                    </li>
                {% else %}
                    <li>
                        <a href="{{ route('merchandise') }}">{{ trans('nav.merchandise') }}</a>
                    </li>
                    <li>
                        <a href="{{ route('discounts.index') }}">{{ trans('nav.discounts') }}</a>
                    </li>
                    <li>
                        <a href="{{ route('login.redirect') }}">{{ trans('nav.log_in') }}</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </nav>
</header>