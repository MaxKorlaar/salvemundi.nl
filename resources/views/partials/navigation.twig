<header>
    <nav class="">
        <div class="container">
            <a href="{{ route('home') }}"><img src="{{ asset('images/logo.svg') }}" alt="Salve Mundi"></a>
            <ul>
                <li>
                    <a href="{{ route('home') }}">{{ trans('nav.home') }}</a>
                </li>
                <li>
                    <a href="{{ route('home') }}">{{ trans('nav.about') }}</a>
                </li>
                <li>
                    <a href="{{ route('signup') }}">{{ trans('nav.sign_up') }}</a>
                </li>
            </ul>
        </div>
    </nav>
</header>
