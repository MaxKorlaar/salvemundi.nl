{% extends 'partials/page.twig' %}
{% block title %}{{ trans('intro.signup.title') }}{% endblock %}

{% block content %}
    <div class="container content">
        <main class="signup">
            <h1>{{ trans('intro.signup.title') }}</h1>
            <div class="row justify-content-center">
                <div class="col-md-4 payment-page">
                    <div class="warning">
                        {{ trans('intro.info.closed_warning')|raw }}
                    </div>
                </div>
            </div>
        </main>
    </div>
{% endblock %}